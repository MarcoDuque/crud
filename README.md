# Crud

## frontend
frontend creado con la verison 9.0.3 de angular-cli

## Start
Una vez el backend se encuentre ejecutado y corriendo en el puerto 3000, se ejecuta el proyecto con `ng serve -o`, si por alguna razon el backend corre en otro puerto se puede configurar el endpoint desde el archivo `app.constants.ts`





